{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Initialize build_runner",
            "type": "shell",
            "command": "dart run build_runner build -d",
            "runOptions": {
                "runOn": "folderOpen"
            },
            "icon": {
                "id": "file-code"
            }
        }
    ]
}